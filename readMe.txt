The program will only work if used in command prompt as it uses a bat file to run

To run the program you can simply double click on the run bat file
or to use it from anywhere you can add this folder to your path environment variables in settings 
which will allow you to call the run bat files from anywhere